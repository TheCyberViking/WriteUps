Memcached Recon: Bruteforcing = recon-cachingserver | Level: Easy  


In this challenge we will look at the dictionary attack on Memcached server.  Please start the lab and answer the following questions:

Questions

Find the password of memcached user "student". Use /usr/share/wordlists/rockyou.txt.gz wordlist.
Find the number of key value pairs on the memcached server.
Find the value stored in key "flag" on the memcached server.
Instructions: 


This lab is dedicated to you! No other users are on this network :) 
Once you start the lab, you will have access to a root terminal of a Kali instance
Your Kali has an interface with IP address 192.X.Y.Z. Run "ip addr" to know the values of X and Y.
The Target machine should be located at the IP address 192.X.Y.3. 
Do not attack the gateway located at IP address 192.X.Y.1 



Answers

Q1. Find the password of memcached user "student".

A. qwerty

Q2. Find the number of key value pairs on the memcached server.

A. 9

Q3. Find the value stored in key "flag" on the memcached server.

A. 61832366e1f912c700181f829b6a0268
